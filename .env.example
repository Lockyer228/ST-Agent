# Copy this file to .env for local defaults. .env is gitignored. Never commit keys.
# Any OpenAI-compatible endpoint works; the UI sidebar form is an alternative.

ST_AGENT_PROVIDER=openai-compatible
ST_AGENT_BASE_URL=https://api.example.com/v1
ST_AGENT_MODEL_ID=
ST_AGENT_API_KEY=
