# ST-Agent Product Source

This repository is the **product source** for ST-Agent. It is deliberately
separate from the shared development control plane, which lives in
`<PROJECT_ROOT>/project/` and holds the approved Plan, collaboration state,
role requirements, and the current handoff.

- Product: turn story ideas into SillyTavern character cards and lorebooks.
- Baseline: `project/Plan/TECHNICAL-ARCHITECTURE.md` and
  `project/Plan/project-plan.md` (approved under C-029).
- Status: initialized by the project lead (Codex) as the repository shell.
  The runtime baseline (WP-01) is established by the implementation role
  (Cursor) in the Windows development environment.

Branches:
- `main` — integration branch, owned by the project lead.
- `impl/cursor` — implementation branch for the Cursor instance.

This placeholder README will be replaced by the runtime README authored in WP-01.
